node_modules/
.next/
.env
.DS_Store
next-env.d.ts
tsconfig.tsbuildinfo
/generated/prisma
.memsearch/

# 조사·기획 원자료와 개인 일정 — 로컬 전용.
# 결론은 README에 반영돼 있고, 원자료에는 사적 메모가 섞여 있어 공개하지 않는다.
docs/03a-research-raw.md
서술형-초안.md

# 에이전트 작업 규칙(CLAUDE.md)은 공개한다 — 이 저장소가 어떻게 만들어졌는지의 증거다.
# 반면 개인 일정은 산출물의 판단 근거가 아니라서 넣지 않는다.
PLAN.local.md

# 참고용으로 받아둔 제3자 게시물 스크린샷. 공개 저장소에 올리지 않는다.
public/seed/IMG_*.PNG

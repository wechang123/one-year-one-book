node_modules/
.next/
.env
.DS_Store
next-env.d.ts
tsconfig.tsbuildinfo
/generated/prisma
.memsearch/

# 타당성 조사 원자료(685줄) — 로컬 전용.
# 결론은 docs/03-feasibility.md에 전부 반영됐고, 그쪽이 정본이다.
# 원자료에는 조사 과정의 사적 메모가 섞여 있어 공개하지 않는다.
docs/03a-research-raw.md

# 진행 계획·일정 — 로컬 전용.
# 에이전트 작업 규칙(CLAUDE.md)은 공개한다. 그건 이 저장소가 어떻게 만들어졌는지의 증거다.
# 반면 개인 일정은 산출물의 판단 근거가 아니라서 넣지 않는다.
PLAN.local.md

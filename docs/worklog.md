# 작업 기록

> AI 도구를 어떻게 다뤘는지, 무엇이 틀렸고 어떻게 발견했는지, 시간이 어디로 갔는지.
> **끝나고 회상해서 쓸 수 없다. 그때그때 남긴다.**

---

## 2026-08-02

### 09:57 저장소 생성
새 저장소를 팠다. 직전에 다른 주제로 같은 구조(Next + Prisma + Docker)를 한 번 만들어봤고,
그때 검증된 인프라 파일을 옮겨 쓰기로 하고 시작했다.

### 10:00 문제 정의 · 사용자 정의
문서 두 개를 15분에 썼다(계획 80분). 다른 주제로 같은 고민을 이미 한 번 해봐서 빨랐다.

**설계에 영향을 준 판단 하나**: 이 서비스는 트리거가 앱 밖에 있다.
*아이가 그림을 들고 오는 것*이 트리거고, 그건 앱이 만들지 않아도 반드시 발생한다.
그래서 **주기·마감·알림 규칙을 아예 만들지 않기로 했다.** 앞선 시도의 최대 약점이 "사용자가 왜 다시 여는가"였는데, 여기서는 그 문제가 구조적으로 없다.

### 10:10 인프라 이식
Dockerfile · docker-compose · entrypoint · Prisma 연결을 직전에 검증해둔 것에서 그대로 옮겼다.
검증이 끝난 것을 다시 만들 이유가 없다. 이름만 치환.

**직접 내린 판단**: `next.config.ts`에 `serverActions.bodySizeLimit`을 8mb로 뒀다.
기본값이 1MB인데 폰으로 찍은 사진은 2~5MB다. 이 설정이 없으면 **업로드가 오류 없이 조용히 실패한다.**
이 서비스는 사진이 콘텐츠라 그 실패가 곧 서비스 전체의 실패다.

### 10:14 ❗ AI가 자기가 어제 발견한 것을 다시 어겼다

**무엇이 문제였나**
스키마를 새로 쓰면서 `datasource db { url = env("DATABASE_URL") }`를 넣었다.
Prisma 7부터는 이게 금지다 — 연결 문자열은 `prisma.config.ts`가 맡는다.

**어떻게 발견했나**
Docker 빌드가 `npx prisma generate`에서 실패했다. 에러 코드 `P1012`.
로컬에서 재현하려 했더니 `node_modules`가 없어서 다른 에러가 났고, **컨테이너 빌드 로그를 직접 다시 잡아** 원인을 확인했다.
`grep`으로 "error"만 걸러서는 안 나왔다 — 빌드 로그는 실패 라인만 요약해서 보여주기 때문에 **해당 스텝의 전체 출력**을 봐야 했다.

**왜 일어났나 — 이게 핵심이다**
어제 **정확히 이 문제를 이미 겪었고**, 커밋 메시지에 이렇게까지 적어뒀다.

> *"Prisma 7은 스키마에 url을 둘 수 없다 — 연결 문자열은 prisma.config.ts가, 실제 연결은 어댑터가 맡는다."*

그런데 새 저장소에서 스키마를 **처음부터 다시 쓰면서** 그 기록을 참조하지 않고 관성대로 썼다.

**교훈**
검증된 설정을 옮길 때는 **다시 쓰지 말고 복사해야 한다.** 인프라 파일은 그대로 복사해서 문제가 없었고,
유일하게 손으로 새로 쓴 파일(`schema.prisma`)에서 사고가 났다.
"이 정도는 기억한다"가 가장 비싼 가정이다.

**시간 손실**: 약 8분

### 10:43 ❗ 시드 문장 12개를 전량 폐기했다

**무엇이 문제였나**
어제 쓴 12문장은 **그림을 확보하기 전에** 썼다. *"코 고는 소리도 그렸는데 안 보여?"* 같은 문장이었다.
그런데 실제로 확보한 CC0 그림은 **수탉·자동차·건물·스페인어 만화**였다. **하나도 안 맞았다.**

**왜 일어났나**
"이미지 확보는 라이선스 조사에 달렸으니, 독립적인 문장 설계를 먼저 하자"고 판단했다.
효율적으로 보였지만 **제품의 순서를 거스른 설계**였다.

> 🔑 부모는 말을 정해두고 그림을 찾지 않는다. **그림을 받고 나서 아이에게 묻는다.**
> 문장을 먼저 쓴 것은 이 서비스가 실제로 쓰이는 순서와 반대였다.

**어떻게 고쳤나**
`public/seed/` 10장을 **한 장씩 실제로 열어보고** 그 그림에 맞는 말을 다시 썼다.
12점 → 10점으로 줄었다. **숫자를 맞추려고 문장을 지어내지 않았다.**

건진 것: 그림을 보고 쓰니 *"그림만 보면 절대 모를 것"*이 목표 3개를 넘어 **7개** 나왔다.
톱니 이빨이 **웃는 것**이고, 얼굴을 가로지른 선이 **이불**이고, 몸통이 없는 건 **옷 때문에 안 보여서**다.
그림 없이 상상해서는 이런 문장이 안 나온다.

**시간 손실**: 어제 문장 작성분 전량. 다시 쓰는 데 약 12분

### 10:46 포트 충돌 — 남의 포트를 뺏지 않기로

`docker compose up -d db`가 *"Bind for 0.0.0.0:5433 failed"*로 죽었다.
범인은 **같은 머신에서 계속 돌고 있던 다른 프로젝트의 DB 컨테이너**였다. 10시간째 5433을 잡고 있었다.

그걸 내리는 대신 이쪽이 비켜갔다 — `.env`에 `DB_PORT=5434`.
**남이 쓰고 있는 포트를 뺏는 대신 이쪽이 옮길 수 있어야 한다**는 게 포트를 환경변수로 뺀 이유였고,
그 설계가 실제로 필요해진 첫 사례다. 요건인 *"포트 환경변수 변경"*을 문서가 아니라 사고로 검증했다.
(`.env`는 gitignore라 심사자는 기본값 5433·3000으로 그냥 돌아간다)

### 10:47 스키마 — 넣지 않기로 한 것들이 판단이다

커밋을 3개로 쪼갰다. 결정 하나에 마이그레이션 하나.

| 안 넣은 것 | 왜 |
|---|---|
| `Artwork.collectionId` | 어느 책에 들어가는지는 이미 `madeOn` 연도가 정한다. 외래키를 따로 두면 *"2026년에 만들었는데 2025년 책에 담긴 작품"* 같은 모순이 만들어질 수 있다. **없는 필드는 틀릴 수 없다** |
| 사진 바이트를 `Artwork`에 | 목록 화면은 바이트가 필요 없다. 같은 테이블에 두면 목록 쿼리가 매번 수 MB를 끌고 온다. `select`로 막을 수는 있지만 **"실수하면 조용히 느려지는" 구조는 만들지 않는다** |
| 주문 가격·쪽수·배송일 | 제작 사양이 안 정해졌다. 지어내면 화면은 채워지지만 근거가 없다 |
| 배송완료 상태 | 요구가 3단계다. 넷째 단계는 배송사 연동 없이 *누가 언제 바꾸는지* 설명할 수 없다 |

넣은 것 중 설명이 필요한 것: `Order.status`와 `OrderEvent`를 **둘 다** 뒀다.
목록은 조인 없이 현재 상태를 읽어야 하고, 상세는 *"언제부터 그 상태였나"*를 보여줘야 한다.
현재 상태만 덮어쓰면 뒤쪽 답이 사라진다.

### 10:50 ❗ 로컬에서 `npm run seed`가 죽었다

*"DATABASE_URL이 없다"*. 컨테이너에서는 compose가 넣어주지만 **로컬 `tsx`는 `.env`를 안 읽는다.**

`dotenv`를 쓰지 않았다. 이 파일은 **프로덕션 컨테이너에서도 실행**되는데 거기서 개발 의존성에 기대고 싶지 않았다.
Node 20.6+의 `process.loadEnvFile()`을 `try/catch`로 감쌌다 — 의존성 0, `.env`가 없는 컨테이너에서도 정상.

### 10:53 주문 1건도 넣지 않기로 — 스키마가 답을 줬다

원래 계획은 *"주문 1건만 넣어 상태 표시를 보여준다"*였다. 스키마를 확정하고 보니 불가능했다.

`Order`는 `Collection`에 딸려 있고, 책은 `@@unique([profileId, year])`다.
→ **주문 1건을 넣으려면 2026년 책을 먼저 만들어야 한다.**
→ 그러면 **심사자가 만들어볼 책이 사라진다.** 눌러볼 게 없던 이전 프로젝트의 실패를 그대로 반복한다.

대신 도달 경로를 짧게 만든다: **책 만들기(버튼 1) → 주문(폼 1) → 상태 변경(버튼 1).**
심사자가 **직접 만든** 주문의 상태가 바뀌는 걸 보는 편이, 남이 만든 주문을 구경하는 것보다 강하다.

### 10:57 ✅ 클린 기동 검증 통과

```
docker compose down -v && docker compose up -d --build
```

| 확인 | 결과 |
|---|---|
| 볼륨 삭제 후 마이그레이션 3개 자동 적용 | ✅ |
| 시드 작품 10점 | ✅ |
| **재시작 후에도 10점** (멱등) | ✅ 3회 연속 실행 + 컨테이너 재시작 |
| 날짜가 밀리지 않음 | ✅ `2026-01-08`이 그대로 |
| 책 0건 · 주문 0건 | ✅ 의도대로 비어 있음 |
| 사진 바이트 합계 | ✅ 1719 kB (원본 1.7MB와 일치) |
| 포트 환경변수 변경 | ✅ 3200 / 5434로 동작 |
| `GET /` | ✅ 200 |

---

## 2026-08-03

### 12:24 공개 전 정리 — 지운 커밋이 오히려 원문을 박제한다

공개로 돌리기 전에 히스토리를 훑었다. `git log --all -p`로 **13건**이 걸렸다.
제3자 회사 실명, 개인 이메일, 그리고 이 저장소 밖의 사정이 적힌 줄들이었다.

**핵심은 이거다** — 그중 상당수가 *"파일에서 지운 커밋"* 안에 있었다.
지우는 커밋의 diff가 삭제된 원문을 그대로 보여준다. **지우는 행위가 박제였다.**
추적 파일만 깨끗하면 된다고 생각했던 게 틀렸다.

`git filter-repo`로 재작성했다. 순서를 정한 근거 하나: **파일 정리를 먼저 커밋하고 재작성했다.**
반대로 하면 정리된 내용이 새 히스토리에 안 들어간다.

**설계가 바뀐 지점**: 처음엔 지정된 6개 문자열만 치환하려 했는데, 실제 줄을 보니
**한 단어를 지워도 같은 줄의 나머지가 문맥을 그대로 복원**했다.
문자열이 아니라 **줄 단위로** 지우는 방식으로 바꿨다.
조사 원자료(685줄)는 치환 대신 **히스토리에서 파일째** 뺐다 — 결론이 `03-feasibility.md`에
이미 다 들어가 있어서 두 벌을 유지할 이유가 없었고, 정본이 둘이면 갈라진다.

검증은 재작성 **후에** 다시 grep해서 0건을 확인했다. 시드 이미지 10장은 sha256을 재작성 전후로
대조했다 — 정규식이 바이너리를 건드리지 않았다는 확인이 필요했다.

**force push로는 안 끝난다.** 옛 커밋이 SHA URL로 계속 열린다.
아무도 보지 않은 Private 저장소였으므로 **원격을 지우고 다시 만들었다.**

**시간**: 약 65분


### 13:09 ❗ 잰 적 없는 값을 "실측했다"고 쓸 뻔했다 — 두 번

**1) 도구가 성공을 반환했는데 실제로는 안 됐다**

브라우저 창을 `375×812`로 줄이는 호출이 **`Successfully resized`를 반환했다.**
그런데 페이지에서 `window.innerWidth`를 찍어보니 **500**이었다.
macOS Chrome이 최소 창 너비로 잘라낸 것이다. **375px에 도달할 방법이 아예 없었다.**

반환값만 믿었으면 *"375px에서 확인했다"*는 틀린 문장을 PR 4개에 전부 남길 뻔했다.

> 🔑 **도구의 성공 반환값은 결과가 아니라 주장이다.**
> 재려던 값을 재고 나서야 잰 것이다.

같은 origin의 **375px iframe**을 띄우고 그 안에서 `matchMedia('(min-width:700px)')`가
`false`인 것까지 확인하는 방식으로 바꿨다. iframe은 진짜 중첩 뷰포트라 미디어쿼리가 375로 평가된다.

**2) 어제 커밋 메시지에 같은 종류의 문장이 있었다**

목록 화면 커밋에 *"1440px에서 4열(카드 322px)임을 실측했다"*라고 써뒀는데,
그때 실제로 측정된 뷰포트는 **1512px**이었다. 리사이즈가 안 먹은 걸 모르고 쓴 것이다.

오늘 진짜 1440에서 다시 쟀다. **결과는 4열·322px로 같았다.**
`.page`의 `max-width`가 1400px이라 그 이상 폭에서는 배치가 동일하기 때문이다.

숫자는 맞고 **출처가 틀린 문장**이었다. 커밋 메시지를 고치려면 force push가 필요한데,
맞는 숫자를 고치자고 히스토리를 다시 흔들 이유가 없다고 봤다.
대신 여기 남긴다 — **정정을 정정으로 남기는 편이, 틀렸던 적이 없는 것처럼 만드는 것보다 낫다.**

### 13:22 ❗ `accept` 속성을 방어라고 부를 뻔했다

등록 폼에 `accept="image/jpeg,image/png,image/webp"`가 있으니 형식 검사는 됐다고 넘어갈 뻔했다.
그건 **갤러리에서 보이는 파일을 줄이는 안내**일 뿐이고, 서버 액션은 폼을 거치지 않고도 호출된다.

말로 넘기지 않고 **실제로 뚫었다.** 내용은 텍스트인데 `type`만 `image/jpeg`라고 주장하는
`File`을 폼에 밀어넣고 제출했다. 매직 넘버 검사가 없었으면 **통과했을 요청**이다.

그래서 형식을 `File.type`이 아니라 **바이트 앞부분에서 읽도록** 바꿨다(`lib/image.ts`).
이 값이 그대로 사진을 내려줄 때의 `Content-Type`이 되므로, 들어오는 자리에서 확정하지 않으면
나가는 자리에서 고칠 기회가 없다.

### 13:30 ❗ "빈 상태를 확인했다"를 코드만 읽고 쓸 뻔했다

시드 10점은 **전부 아이 말이 있어서** 빈 분기가 한 번도 렌더된 적이 없었다.
코드를 읽고 "되겠지"로 넘어갈 뻔했고, 대신 DB에서 한 건을 `NULL`로 만들어 **실제로 렌더시킨 뒤**
원문으로 되돌리고 `lib/seed-data.ts`와 문자열 대조해 복구까지 확인했다.

제출 중 버튼 상태도 처음엔 못 잡았다 — 150ms 뒤 한 번만 표본을 떴는데 이미 페이지가 넘어간 뒤였다.
**"확인했다"고 쓰는 대신** 25ms 간격으로 100회 폴링해 `"저장하는 중…" disabled=true`를
관찰하고 나서 적었다.

### 13:36 네 화면 — 이슈 → 브랜치 → PR → squash merge

여기서부터 `main`에 직접 커밋하지 않았다. 이슈 4개, PR 4개, 전부 squash merge.
**판단과 거부한 선택지는 PR 본문에 남겼다.** 커밋 메시지는 짧고, 왜 그 갈림길에서
그쪽을 골랐는지는 PR에 있다.

투입 대비 회수 순으로 갔다. 404를 **가장 먼저** 만든 것이 그 판단이다 —
홈의 클릭 대상 11개(등록 버튼 1 + 카드 10)가 전부 404로 떨어지고 있었고,
그 404엔 홈으로 가는 링크조차 없었다. **파일 하나로 11개가 한꺼번에 고쳐졌다.**

### 13:43 ✅ 클린 클론 게이트 통과 — 로컬 경로가 아니라 GitHub URL에서

이전 검증은 `origin`이 로컬 경로였다. **제출물을 검증한 게 아니었다.**
이번엔 `git clone git@github.com:...`으로 받아서 돌렸다.

한 가지를 더 발견했다. `docker-compose.yml`의 `name:`이 고정이라 볼륨 이름도 고정되는데,
그대로 `up` 하면 **마이그레이션과 시드가 이미 끝난 기존 볼륨을 재사용한다.**
심사자 PC에는 그 볼륨이 없으므로 그건 클린 검증이 아니다.
별도 프로젝트 이름으로 **빈 볼륨**을 만들어 처음부터 돌렸다.

| 확인 | 결과 |
|---|---|
| `docker compose up --build` 1회 | ✅ `.env` 없이 기동 |
| 빈 볼륨에서 마이그레이션 3개 적용 | ✅ |
| 시드 작품 10점 · 책 0 · 주문 0 | ✅ |
| 사진 바이트 합계 | ✅ 1719 kB |
| **기본 포트** `3000` | ✅ |
| **포트 변경** `PORT=8080 DB_PORT=5555` | ✅ `8080->3000`, `5555->5432` |
| 목록 → 등록 → 상세 → 편집 → 목록 왕복 | ✅ 브라우저로 실제 클릭 |
| 1440px 목록 | ✅ 4열 · 카드 322px · 이미지 10장 전부 로드 |

`DB_PORT`가 왜 있는지도 여기서 한 번 더 드러났다 —
검증 중에도 5433은 **다른 프로세스가 잡고 있었다.** 그걸 내리는 대신 이쪽이 비켜갔다.

### 13:54 ❗ 저장소를 지웠더니 이슈와 PR이 같이 사라졌다 — 오늘 가장 비싼 실수

Public 전환 직전 마지막 검사에서 **놓친 것 두 가지**가 나왔다.

**하나는 내 순서 실수였다.** `.gitignore`와 `worklog.md`의 일정·대안 관련 줄을
히스토리 재작성 **뒤에** amend로 고쳤다. 현재 파일은 깨끗한데 히스토리엔 원문이 남았다.
*"정리를 먼저 커밋하고 재작성한다"*를 아침에 배워놓고 오후에 어겼다.

**둘은 커밋 작성자 이메일**이었다. 34개 항목에 개인 메일이 박혀 있었고,
Public이 되면 그대로 공개된다. `--mailmap`으로 GitHub noreply로 통일했다.

둘 다 히스토리 재작성이 필요했고, force push로는 옛 커밋이 SHA로 계속 열리므로
**저장소를 지우고 다시 만들었다.** 여기서 대가를 치렀다.

> 🔴 **저장소를 지우면 이슈와 PR도 지워진다.**
> 첫 번째 삭제 때는 이슈·PR이 없어서 무해했고, 그 경험이 두 번째 판단을 무디게 만들었다.
> *"해본 적 있는 안전한 작업"*으로 분류한 순간, **그 사이에 무엇이 쌓였는지를 안 봤다.**

날아간 것은 화면 4개 + README의 PR 본문이다. 거기에 갈림길과 버린 선택지,
그리고 AI 제안 중 거부한 것과 그 이유가 들어 있었다 — 이 저장소에서 **코드보다 설명하기 어려운 부분**이다.

**복구**: 본문을 되살려 `docs/decisions/`에 넣었다. 되살리면서 **PR보다 저장소 안이 맞는 자리**라고
판단이 바뀌었다 — `clone`만 하면 보이고, 저장소를 다시 만들어도 안 사라지고, 코드와 같은 이력을 탄다.
커밋 메시지에 남은 `(#5)`~`(#9)`는 그때 사라진 PR을 가리키는 죽은 링크다. 지우지 않고 둔다.

**교훈**: 되돌릴 수 없는 작업은 *"전에 해봤다"*가 아니라 **"지금 무엇이 걸려 있나"**로 판단해야 한다.
같은 명령이라도 두 번째에는 잃을 것이 생겨 있었다.

**시간 손실**: 약 25분

### 14:01 ❗ 검사 스크립트가 파일 하나를 건너뛰고 있었다

오염 검사를 `git ls-files | while read f; do grep … "$f"; done`으로 돌리고 있었다.
그런데 git은 **한글이 든 파일명을 이스케이프해서** 출력한다.
그래서 `docs/04b-대안-생성이미지(채택안함).md`는 **grep에 도달조차 못 했다.**
*(그 파일은 나중에 `docs/04b-생성이미지.md`로 이름이 바뀌었다. 이 문단은 당시 이름 그대로 둔다)*

알아챈 계기는 **알고 있던 1건이 결과에 안 나온 것**이었다.
04b에 있는 걸 아는 문자열을 검사했는데 0건이 나왔다. 0이 맞을 리가 없었다.

> 🔑 **검사 결과가 0일 때, 검사가 동작했다는 증거가 따로 필요하다.**
> *나오는 게 없는 것*과 *보지 않은 것*은 결과 화면에서 똑같이 생겼다.
> 그래서 "반드시 걸려야 하는 것"을 하나 알고 들어가는 게 값이 싸다.

`git grep`으로 바꾸니 정상적으로 잡혔다. 그 전까지의 *"0건"*은 전부 **한글 파일명 1개를 뺀 0건**이었다.

### 17:30 저녁 재개 — 먼저 "이미 깨져 있는 것"부터

새 기능(Lv2)보다 **지금 깨져 있는 것 세 건**을 먼저 고쳤다.
셋 다 *"동작한다고 적혀 있는데 실제로는 아닌"* 종류라, 그 위에 기능을 얹으면
**새로 만든 것도 같은 종류의 거짓이 된다.**

**시간**: 블록 A 약 9분

### 17:36 ❗ 크기 검사가 도달할 수 없는 코드였다 — 그리고 그걸 증명했다

`next.config.ts`의 `bodySizeLimit`과 `actions.ts`의 `MAX_BYTES`가 **둘 다 8MiB**였다.
그런데 앞의 것은 **파일이 아니라 요청 본문 전체**에 걸린다 —
multipart 경계, 필드 이름, 아이 말, 만든 날까지 포함이다.

그래서 8MiB를 넘는 파일은 **본문 단계에서 먼저 잘려 액션이 시작조차 못 했고**,
`"사진이 너무 큽니다"`는 **한 번도 실행된 적 없는 코드**였다.
화면은 `8MB까지`라고 안내하면서 **경계에서 거짓말**을 하고 있었다.

고치는 것보다 **고쳤다는 걸 증명하는 게** 어려웠다.
진짜 JPEG 헤더(`ff d8 ff`)로 시작하는 **8,936,491B** 파일을 만들어 제출했고,
그다음 **설정을 원래대로 되돌려 같은 파일을 다시 넣었다.**

| `bodySizeLimit` | 결과 |
|---|---|
| `"8mb"` (원래) | 한국어 문구 **0건** · 오류 경계로 떨어짐 |
| `"12mb"` (고친 뒤) | `사진이 너무 큽니다 (8.5MB). 8.0MB까지 올릴 수 있어요.` |

> 🔑 **되돌려서 재현하는 데 든 시간이 고치는 데 든 시간보다 길었다.**
> 그런데 그 실행이 없었으면 *"죽은 코드였다"*는 문장이 **추측**이었다.

덤으로 이 실행이 **실패 화면(`error.tsx`)의 필요성도 같이 증명했다** —
설정을 되돌린 상태의 요청이 정확히 그 화면으로 떨어졌다. 그게 없었으면 사용자는 빈 화면을 봤다.

### 17:38 ❗ [다시 시도] 버튼이 복구를 못 했다

`error.tsx`의 표준 형태는 `onClick={reset}`이다. 그대로 넣었다.
**그리고 눌러봤다** — DB를 껐다 켠 뒤에도 **오류 화면이 그대로 남고 카드가 0개**였다.

`reset()`은 *이미 받아둔 데이터로 경계 안쪽을 다시 그릴 뿐*이다.
여기서 실패한 건 서버 컴포넌트의 쿼리라, 서버에서 다시 받아오지 않으면 같은 실패가 반복된다.

> 🔑 **동작하지 않는 [다시 시도]는 없는 것보다 나쁘다.**
> 눌러본 사용자가 *"복구가 안 되는 상황"*이라고 잘못 배운다.
> **표준 형태를 넣었다는 것과 그게 이 화면에서 동작한다는 것은 다른 이야기다.**


### 17:55 ❗ 오류가 나면 사용자가 친 값이 전부 사라졌다

주문 폼에서 이름·주소를 넣고 **연락처만** 틀리게 제출했다.
오류 문구는 정확히 떴는데 **세 칸이 전부 비어 있었다.** React 19가 폼 액션 뒤 입력칸을 초기화한다.

성공 뒤에는 맞는 동작이다. **오류로 되돌아왔을 때는 주소까지 다시 치게 만든다.**

더 나쁜 건 **이미 머지된 작품 등록·편집 폼에도 같은 결함이 있었다**는 것이다.
`docs/decisions/03`에 *"실수 경로를 **전부** 실제로 제출해서 확인"*했다고 적어뒀는데,
**오류 문구가 뜨는 것만 봤고 그 다음에 무엇이 남는지는 안 봤다.**

> 🔑 **거짓은 아니었다. 좁았을 뿐이다. 그런데 좁은 확인은 넓은 확인처럼 보인다.**
> `전부`라는 단어가 그 차이를 지웠다. 그 단어를 빼고 무엇이 빠졌는지를 남겼다.

### 18:00 ❗ 한글 날짜에 영문 `PM`이 섞였다

주문 이력의 시각이 `2026년 8월 3일 PM 5:54`로 나왔다. 로케일은 `ko-KR`인데도 그랬다.

취향 문제로 보고 넘길 뻔했다. 진짜 문제는 **실행 환경의 ICU 로케일 데이터 구성에 따라
다른 글자가 나온다**는 것이고, **그건 화면에서 티가 안 난다.**
`hour12: false`로 고정해 그 변수를 없앴다.

같은 자리에서 하나 더 잡았다 — `formatMadeOn`을 `createdAt`에 쓸 뻔했다.
그건 `@db.Date` 전용으로 **UTC에 고정**해둔 함수라, 진짜 시각에 쓰면 9시간 전으로 보이고
**자정 근처에서는 날짜가 하루 밀린다.**

### 18:10 Lv2 — 주문 기능 다섯 개

책 만들기 · 주문 넣기 · 목록·상세 · 상태 전진 · 데모 초기화.
전부 이슈 → 브랜치 → PR → squash merge로 넣었고, 판단은 PR 본문과 `docs/decisions/`에 있다.

**설계에서 오래 걸린 것 두 가지**

**① 주문번호를 어떻게 만들 것인가.** `cuid`는 *"주문번호가 어떻게 되세요"*에 답할 수 있는 값이 아니다.
`20260803-7K2M` 형태로 정했는데, **세 결정이 전부 "전화로 부른다"에서 나왔다** —
날짜가 앞에 오고(언제 넣었는지 번호만 보고 안다), 뒤는 네 글자만(더 길면 불러주다 틀린다),
`0 O 1 I L`을 뺐다(되묻게 되는 글자다).

**② 중복 클릭을 어디서 막을 것인가.** 버튼 잠금이 이미 있으니 됐다고 넘어갈 뻔했다.
그건 **아침에 `accept` 속성을 방어라고 부를 뻔한 것과 정확히 같은 형태**다.
버튼은 *두 번 누르는 일 자체*를 줄이고, 서버는 *두 번 눌렸을 때 두 건이 생기는 것*을 막는다 —
새로고침 후 재전송은 버튼이 관여할 수 없다.

**시간**: 블록 B 약 32분

### 18:20 문서 정합성 — 내가 쓴 문장 두 개가 틀려 있었다

**① `docs/decisions/03`의 "전부"** — 위 17:55 참조.

**② `docs/decisions/README`의 "(#5)~(#9)는 죽은 링크다"** — **확인하지 않고 기억으로 쓴 문장**이었다.
실제로는 그 뒤에 **번호가 재사용되면서 더 나빠졌다** — 새 PR이 `#5`를 받아
`feat: 404 화면 …(#5)`가 **엉뚱한 PR을 가리키게** 됐다. 죽은 링크보다 나쁘다.

옛 참조 다섯 개를 지웠는데, 이번에는 **저장소를 다시 만들지 않았다.**

> 🔑 옛 커밋에 지워야 할 내용이 없어서 SHA로 남아도 무해했고, **그러면 force push로 충분하다.**
> 앞선 두 번은 *비밀을 지우는 것*이라 삭제·재생성이 필요했고, 이번은 *표기를 고치는 것*이라 아니었다.
> **같은 목적이라도 무엇을 지우려는지에 따라 필요한 도구가 다르다** —
> 두 번째 삭제 때 이 구분을 못 해서 이슈·PR을 날렸다.

**③ `app/page.tsx`의 쿼리에 `where`가 없었다.** `schema.prisma`는
`@@index([profileId, madeOn])`로 *"이 아이의 작품을 만든 날 역순"*을 전제로 인덱스를 깔아뒀는데
**그 쿼리가 존재하지 않았다.** 아이가 한 명뿐이라 화면은 같아 보였다.
**주석이 코드보다 앞서 있는 상태**라 책 기능(#6)에서 같이 고쳤다.

### 19:05 ✅ 최종 클린 클론 — README가 설명한 실패가 그대로 재현됐다

`git clone git@github.com:…`으로 받아 **빈 볼륨**으로 돌렸다.

**기본값 그대로 돌렸더니 실패했다** — `Bind for 0.0.0.0:5433 failed: port is already allocated`.
이 머신의 5433은 여전히 다른 프로젝트가 잡고 있다. **README가 설명한 그 상황이 그대로 나왔고,
README가 안내한 해결책(`DB_PORT=5599`)으로 그대로 떴다.**

> 🔑 문서에 적어둔 실패를 **실제로 겪어보고 적은 것**과 상상해서 적은 것은 다르다.
> 이번 검증은 그 문장이 맞는지까지 확인한 셈이 됐다.

| 확인 | 결과 |
|---|---|
| 빈 볼륨에서 마이그레이션 3개 | ✅ |
| 시드 10점 · 책 0 · 주문 0 | ✅ |
| 기본 포트 `localhost:3000` | ✅ |
| 목록 → 등록 → 편집 → 책 → 주문 → 상태 3전진 → 초기화 | ✅ 브라우저 실제 클릭 |
| 1440px | ✅ 4열 · 카드 322px · 이미지 10/10 |
| 404 화면 | ✅ `이 주소에는 아무것도 없어요.` |

### 19:05 ❗ "이미지가 안 뜬다"가 두 번 다 앱 문제가 아니었다

검증 중 **홈에서 이미지가 하나도 안 뜨는** 상황을 두 번 만났다. 둘 다 원인이 앱 밖이었다.

**① 다른 프로젝트의 서비스워커가 `/`를 가로챘다.**
탭 제목이 `YuWiChang`으로 떴다. `localhost:3000`에 예전 포트폴리오 사이트의
서비스워커(`ywc-portfolio-v3` 캐시)가 등록돼 있었다. **`curl`로는 정상 응답이 나왔다.**

**② 배경 탭에서 `loading="lazy"`가 지연됐다.**
`fetch`로는 200이 오는데 `<img>`는 `complete: false`였다. `document.visibilityState`를 찍어보니 **`hidden`**.
`loading="eager"`로 바꾸니 즉시 `1194x1843`으로 로드됐다.

> 🔑 **"화면에 안 보인다"는 증상이지 원인이 아니다.**
> 두 번 다 앱을 의심하고 코드를 뒤질 뻔했다.
> `curl`로 서버 응답을 먼저 보고, `visibilityState`처럼 **측정 환경 자체를 찍어보는 것**이
> 코드를 읽기 전에 할 일이었다.

**못 한 것**: 이 상태(창이 화면에 안 보임)에서는 **캡처에 이미지가 안 담긴다.**
레이아웃 수치는 전부 실측했지만 PT용 캡처는 창을 띄운 상태에서 따로 찍어야 한다.

---

## 2026-08-04 — 전제가 바뀌었다

**그림 한 장에서 아이의 시간 전체로.** 초음파 사진부터 초등 저학년까지, 타겟에 임산부까지.
이날 한 일은 **기능을 더한 것이 아니라 기존 결론을 다시 유도한 것**이다.

### 14:13 먼저 "지금 이미 깨져 있는 것"부터

넓히기 전에 고쳤다. **넓히고 나서 고치면 새로 만든 것이 범인으로 보인다.**

### 14:20 ❗ 주문 중복 열쇠가 연락처만 보고 있었다

같은 요청인지 판정하는 곳이 **두 군데인데 보는 값이 서로 달랐다.**

| | 무엇을 봤나 |
|---|---|
| 조회(`findFirst`) | 책 · **이름** · 연락처 · **주소** · 60초 |
| 유니크 열쇠(`dedupKey`) | 책 · 연락처 · 시간버킷 |

받는 사람과 주소가 달라도 연락처가 같으면 둘째 주문이 유니크 제약에 걸렸고,
코드는 그 충돌을 *"동시 제출"*로 해석해 **첫 주문의 번호로 보내버렸다.**
화면은 `접수됐습니다`라고 말하고 **DB에는 한 건뿐이고 로그는 조용하다.**

같은 연락처로 이름·주소만 다르게 두 번 제출해서 확인했다 — 고친 뒤 **2건이 남고**,
글자 하나 안 바꾼 재제출은 **여전히 2건**이다(막아야 할 것은 그대로 막힌다).

> 🔑 교훈은 *"필드를 빠뜨렸다"*가 아니라 **같은 판정을 두 곳에서 따로 정의했다**는 것이다.

### 14:25 ❗ 검색이 홈의 연도 집계를 오염시켰다

책 줄의 *"지금까지 N점"*을 **검색 필터가 걸린 목록에서 세고 있었다.**
`?q=닭`이면 `지금까지 1점`이라 말하고, 그 자리에서 [책으로 묶기]를 누르면 **10점짜리 책**이 만들어진다.
0건 검색이면 **책 줄이 통째로 사라졌다.**

고칠 때 *"검색 중일 때만 추가 조회"*를 안 골랐다 — **경로가 둘이 되면 갈라지고,
방금 갈라져서 이 버그가 났기 때문이다.**

### 14:40 시간 축 — 생년월일을 받기로 뒤집었다

전에는 *"이 나이면 보통 어느 정도인가요"*라는 다음 질문 때문에 안 받았다.
**그 판단은 날짜의 쓸모가 나이 표시 하나뿐일 때 맞았다.**
지금은 임신 주차·생후 개월·만 나이가 **전부 이 날짜에서만 나온다.**

피하는 대신 **받고, 그 질문에 화면이 직접 답하게** 했다 — *"여기서는 답하지 않습니다"*와 왜 그런지.

날짜 칸을 **둘로 나눈 것**(`출산예정일`·`태어난 날`)이 이날의 정확성 판단이다.
임신 주차는 출산일이 아니라 임신 시작에서 세므로, 태어난 날을 40주 0일로 놓고 거꾸로 세면
**24주에 찍은 초음파가 26주로 표시된다.** 없는 것보다 나쁘다.

### 14:47 ❗ [처음 상태로 되돌리기]가 새 열을 안 되돌렸다

`quoteBy`를 스키마·액션·화면까지 다 연결하고 **시드에는 안 넣었다.**
값을 바꾼 시드 행을 두고 초기화를 눌렀는데 **안 돌아왔다** —
`origin`이 `SEED`라 삭제 대상이 아니고, 재적용 `update`가 그 열을 안 건드렸다.

```
초기화를 눌렀는데:  child 9 | parent 1
고친 뒤:            child 10 | parent 0
```

**초기화가 "USER 삭제 + 시드 재적용"인 이유가 정확히 이건데 재적용 쪽이 샜다.**
발견 계기는 *"고쳤으니 되돌려두자"*로 누른 초기화였다. **정리하려다 버그를 밟았다.**

### 15:00 ❗ 빈 홈에 검색 칸이 떠 있었다

자기설명 UI를 판정하려고 `DELETE FROM "Artwork"` 후 홈을 실제로 열어봤다.
**찾을 것이 하나도 없는데 `아이가 한 말로 찾기` 입력칸이 빈 화면 위에 있었다.**

문구 자체는 여섯 개 빈 상태가 다 통과했다. **틀린 것은 그 위였다.**
빈 화면은 안내할 자리가 가장 넓은 화면인데 그 자리를 검색이 먹고 있었다.

> 🔑 **빈 상태는 코드를 읽어서 판정할 수 없다. 비워놓고 열어봐야 한다.**
> 문구만 확인하고 넘어갔으면 못 잡았다.

### 15:15 시드를 생성물로 — 04b를 뒤집었다

`docs/04b`가 *"AI 생성은 채택하지 않았다"*고 기록돼 있었다.
**초음파 사진과 상장은 CC0로 구할 수 없다** — 실제 출력지에는 산모 이름·병원명이,
상장에는 아이 실명과 학교명이 있고, 구해지더라도 **실존하는 누군가의 임신 기록**이다.

접었던 세 이유를 **하나씩 판정**했다. 둘은 살아 있고 하나만 뒤집혔다 —
*"화풍 통제가 함정이다"*는 **한 해**를 볼 때 맞았고, 지금은 **8년**을 본다.

⚠️ **이미지 모델을 안 썼다.** 같은 프롬프트로 같은 그림이 다시 안 나오면
**저장소가 자기 시드를 다시 만들 수 없다.** 코드로 그리면 난수까지 고정 시드라 같은 바이트가 나온다.
그래서 README에 *"생성형 AI로 만들었습니다"*라고 **쓰지 않는다** — 픽셀을 만든 것은 코드다.

### 15:20 ✅ 밟기 전에 막은 것 하나

시드를 12점으로 갈아엎으면 **옛 10점의 id가 목록에서 사라진다.**
`upsert`만 돌리면 옛 행은 `origin = SEED`라 초기화에도 안 지워지고 **22점짜리 데모**가 된다.
`applySeed`가 **목록에 없는 SEED 행을 먼저 지우게** 했다.

바로 앞(14:47)에서 같은 종류를 **밟고 나서** 고쳤기 때문에 이번엔 먼저 보였다.
*"시드를 적용한다"*는 곧 **목록과 DB를 같게 만든다**는 뜻이어야 한다.

### 19:00 화면을 처음 보는 눈으로 다시 봤다 — 먼저 재고 나서 고쳤다

*"사이트가 너무 무지 느낌"*이라는 지적을 받았다. **인상을 인상으로 고치면 안 되니 먼저 쟀다.**

```
페이지 폭                 1400px
머리말 글 폭               435px  → 오른쪽 버튼까지 빈칸 805px
검색 입력칸 폭            1263px  (찾는 말은 "공룡" 두 글자다)
책 줄 글 끝 ~ 버튼 사이     998px
홈에 렌더된 글자          먹 498자 / 회색 463자 / 종이색 5자  → 셋 다 무채색, 회색 48%
--accent                #1c1a17  =  --ink와 같은 값
```

**진단이 인상과 달랐다.** "밋밋하다"의 실체는 취향이 아니라 두 가지였다 —
① 강조색이라는 토큰이 본문 색을 들고 있어서 **화면에 유채색이 아이 그림 말고 없었고**,
② `.grid`에 *"넓어지면 칸이 커지는 게 아니라 더 많이 보인다"*고 적어놓고
**머리말·검색·책 줄은 그 원칙을 어기고 있었다.** 격자만 지키고 있었던 것이다.

### 19:20 ❗ 8년치가 구분선 하나 없이 이어져 있었다

격자 4번째 칸이 `만 3세 2개월`, 5번째가 `생후 6개월`이었다. 그 사이에 아무 표시가 없다.
시드가 한 해(10점)에서 네 해(12점)로 넓어질 때 **격자만 그대로 남은 것**이다 —
한 해짜리 목록에서는 구분이 필요 없었으니, 구분을 안 만든 것은 판단이 아니라 잔해였다.

해 단위로 묶었다. **새 컬럼도 새 질문도 없다** — `describeAge`가 이미 계산하던 값이다.
달 단위로 더 자르지 않은 이유는 immich에 올라온 것과 같다(점이 적고 여러 해에 흩어지면
제목만 남고 아래가 빈다). 원래 요구는 *"그림이면 그림, 상장이면 상장"*이었지만
**시드 12점에서 시기가 종류를 거의 결정한다**는 것을 확인하고 `⛔ 종류를 묻지 않는다`를 지켰다.

### 19:28 ❗ 상단 바를 얹었더니 폰에서 그림이 100px 내려갔다

`layout.tsx`가 `<body>{children}</body>` 한 줄이라 화면 14개 사이를 옮기는 길이 전부
문장 속에 숨어 있었다. 44px짜리 줄을 하나 얹었는데, 375px 홈에서
**첫 그림이 y=421 → 522(화면의 71%)로 밀렸다.**

빚을 그 PR 본문에 적어두고 다음 PR에서 갚았다 — 검색 폼을 `<details>`로 접고(142px),
`남긴 것 12점 · 되짚어보기` 줄을 지웠다(세 조각이 전부 다른 곳으로 갔다). **428px로 돌아왔다.**

작업 중에 `ReferenceError: Camera is not defined`를 냈는데, **상단 바가 오류 화면 위에
그대로 떠서 나갈 길이 있었다.** 의도한 동작이 우연히 검증됐다.

### 19:30 ❗ 아이콘을 하나 넣고 렌더해 보고 갈아치웠다

`되짚어보기`에 lucide `quote`(따옴표 두 개)를 넣었다. 14px로 실제 화면에 그려보니
**`))` 두 글자로 읽혔다.** 말풍선으로 바꿨다. 따옴표는 글자 옆에 있을 때만 따옴표다.

`.search__toggle`도 같은 종류였다 — `display: flex`를 걸었더니 브라우저가 그리던
**펼침 삼각형(`::marker`)이 사라져서** 접혀 있다는 표시가 하나도 없었다.
🔑 **둘 다 코드로는 멀쩡했고 렌더해서만 보였다.**

### 19:35 라이선스 — 조사하고 두 번 접었다

**iconsax**를 조사했다. 아이콘 그림이 *"다른 이름으로 재배포 금지, 템플릿·UI킷은 허가 필요"*로
공지돼 있는데 **전문을 못 읽었다** — `docs.iconsax.io/license`가 본문을 스크립트로 그려서
받아지지 않았고, npm이 가리키는 저장소는 `GET /repos/…/license`가 **404**다.
Lucide(ISC)·Phosphor(MIT)는 `LICENSE` 파일을 직접 받아 확인했다. **읽은 것만 채택했다.**

**Pretendard**(SIL OFL)도 넣으려다 크기를 재고 접었다 — variable woff2가 **2,009KB**다.
이 저장소는 목록 쿼리에서 사진 바이트를 뺐고 그 이유가 *"10점만 있어도 매 요청이 1.7MB"*였다.
**1.7MB를 비싸다고 해놓고 2MB를 글꼴로 얹으면 앞뒤가 안 맞는다.**
대가(OS마다 글꼴이 달라 줄바꿈이 달라질 수 있다)는 README에 적었다.

### 20:40 ❗ 검색을 접은 것이 틀렸다 — 아낀 102px 중 8px만 토글 덕분이었다

*"검색 기능은 왜 토글로 한 거지?"*를 듣고 다시 쟀다. 168px을 아꼈다는 것까지는 맞는데
**그 168px을 안 쪼개봤다.**

```
이름표 줄 42 + 입력칸 줄 50 + 도움말 줄 42 + 여백 24  =  168
접었을 때                                            =   66
입력칸 줄만 남겼을 때                                  =   74   ← 차이 8px
```

**8px을 벌자고 기능 하나를 클릭 뒤에 숨긴 셈**이었고, 부 사용자는 세로가 남아도는
1440px 노트북인데 폰과 같이 접혀 있었다. 이름표는 자리표시자로 내리고 도움말은
검색 중에만 띄워 되돌렸다.

🔑 **"얼마를 아꼈나"는 쟀는데 "무엇이 그 값을 만들었나"는 안 쟀다.**
합계만 보면 어떤 해법이든 좋아 보인다. 이번 것은 쪼개는 순간 답이 뒤집혔다.

### 21:40 ❗ 한국어 화면에서 `ch`는 쓸 수 없는 단위다

*"레이아웃도 간결하지가 않아"*를 듣고 머리말을 쟀다 — 144px에 4줄이었고, 첫 그림 위에
**전폭 가로선이 3개**였다. 겹치는 것들을 걷었다: 좌우로 갈려 있던 같은 성격의 메타 줄 둘을
한 줄로, `.sitenav`와 45px 간격으로 같은 선을 긋던 `.masthead` 밑줄 제거,
테두리와 그림자로 경계를 두 번 말하던 카드에서 테두리 제거, 상자 안의 상자였던 책 구역.

그 과정에서 lede를 한 줄로 만들려고 `max-width: 46ch` → `64ch`로 올렸는데
**여전히 두 줄이었다.** `ch`는 숫자 `0` **한 글자의 폭**이고 한글은 그 두 배 가까이 넓다 —
`64ch`가 "64자"가 아니라 대략 **34자**였다. 원래 값 `46ch`도 46자가 아니라 24자쯤이었다.

🔑 **이 착각은 코드만 읽어서는 안 보인다.** `ch`는 영문 기준으로는 맞는 단위고,
문서에도 "글자 수"라고 설명돼 있다. **한국어 화면에 렌더해야만 드러난다.**
렌더된 폭(약 666px)을 재서 `46rem`으로 바꿨다.

결과: 머리말 144→98px · lede 51→26px · 전폭 가로선 3→2 · 첫 그림 y 377→326(폰 437→383).

### 22:30 외부 디자인 지침을 **명령이 아니라 검사 항목**으로 읽었다

[`Leonxlnx/taste-skill`](https://github.com/Leonxlnx/taste-skill)(MIT)을 받아 우리 화면에 댔다.
남이 쓴 문서가 "이렇게 하라"고 적혀 있다고 그대로 하면
*"설명할 수 없는 코드를 채택하지 않는다"*가 그 자리에서 깨진다. 그래서 절차를 뒀다 —
**항목만 뽑고 → 우리 화면을 재고 → 잰 값이 문제를 보일 때만 고친다.**

🔑 **값이 컸던 것은 고치는 방법이 아니라 재는 항목이었다.**
`간격 값이 몇 종인가` · `한 줄에 구분자가 몇 개인가` · `포커스 링이 몇 곳에 설계돼 있나`는
화면을 아무리 오래 봐도 안 보이고 **세면 즉시 보인다.** 세어보니:

```
간격 rem 값        29종   ← 0.4rem과 0.45rem이 따로 있었다 (0.8px 차이)
:focus-visible      2곳   ← 격자 카드 12칸이 전부 설계 밖
:active             0곳   ← 폰에는 hover가 없어 터치 피드백이 0이었다
<main>            0파일
사용자 문구의 —     8곳
```

**안 따른 것 여섯 개에도 근거를 적었다**(`docs/decisions/13-taste-audit.md`).
Lucide를 바꾸라는 항목은 근거가 미학인데, 이 저장소가 아이콘을 고른 축은
**라이선스 전문을 확인할 수 있는가**였다. 축이 다르면 따르지 않는다.

### 22:52 ❗ 파비콘이 깨졌는데 `curl`은 200을 줬다

`app/icon.svg`를 만들고 확인했더니 **200 · `Content-Type: image/svg+xml`**이라 통과로 봤다.
`<img>`로 실제로 그려보니 **네 크기 전부 깨진 이미지**였다.

주석에 CSS 변수 이름을 적었는데 **XML 주석 안에는 붙임표를 두 개 연달아 쓸 수 없다.**
변수 이름이 하필 그 모양이라 파일이 XML로서 무효가 됐고,
**브라우저는 오류를 내지 않고 깨진 이미지만 보여준다.**

🔑 **서버가 정상 응답을 한다는 것과 그 내용이 유효하다는 것은 다른 사실이다.**
이 저장소가 *"결과가 0일 때 검사가 동작했다는 증거가 따로 필요하다"*를 배운 것과 같은 계열인데,
이번은 **결과가 200이라 더 잘 속았다.** `xml.dom.minidom.parse`로 유효성 검사를 붙였다.

같은 작업에서 전역 포커스 규칙에 `border-radius: inherit`을 넣었다가 뺐다 —
`outline`은 이미 요소 반지름을 따라가는데, 다시 지정하면
**버튼이 포커스되는 순간 부모의 반지름(0)으로 바뀐다.** 링을 고치려다 버튼을 네모로 만드는 코드였다.

### 23:20 🔴 저장소 자기 규칙을 과제 요건인 것처럼 써서 후보를 죽이고 있었다

*"`설명할 수 없는 코드를 채택하지 않는다`가 과제 가이드라인에 있어? 없는 것 같은데.
그리고 적용을 해서 설명을 할 수 있게 연습을 하면 되잖아"* 라는 지적을 받았다.

`grep`으로 전수 확인했다 — 그 문구는 **`CLAUDE.md` 한 곳에만** 있다. 내가 받은 작업 규칙이지
과제 요건이 아니다. `PLAN.local.md`에 요약해둔 실제 채점 기준은 이렇게 말한다:
*"그 판단을 설명할 수 있는가."* **설명할 수 있게 되라는 요구**이지 설명 못 하는 것을
피하라는 요구가 아니다.

🔑 **나는 학습 요구를 거부권으로 바꿔 쓰고 있었다.**
*"설명할 줄 모른다"*와 *"채택할 값이 없다"*는 다른 말인데, 앞을 뒤인 것처럼 적으면
**후보를 재보지도 않고 죽일 수 있고 그게 신중해 보인다.** 그게 이번에 실제로 일어난 일이다.

### 23:35 그 논리로 물린 것을 다시 재보니 309KB였다

웹폰트를 접으며 이렇게 썼었다 —
*"직접 서브셋하면 300KB 안쪽으로 줄일 수 있지만, 그러려면 서브셋을 만드는 코드가
저장소에 있어야 하고 그 값이 지금의 대가보다 크지 않다."* **뒷문장은 측정이 아니라 회피다.**

그리고 앞 근거도 틀렸다. *"1.7MB를 비싸다고 해놓고 2MB를 글꼴로 얹으면 앞뒤가 안 맞는다"* —
**1.7MB는 매 요청이고 글꼴은 한 번 받고 캐시된다.** 비교할 수 없는 둘을 나란히 놓으니
문장이 그럴듯했다. 단위가 다른 두 수는 나란히 놓는 것 자체가 결론을 만든다.

실제로 재본 값:

```
원본                                          2,009KB
+ 한글 음절 전체(11,172자)                     1,701KB
+ KS X 1001 완성형(2,350자)                      438KB
+ 가변축 wght 400~700 제한                       309KB
```

🔴 처음에 KS X 1001을 **파이썬 `euc-kr` 코덱으로 걸렀다가 틀린 값을 얻었다.**
그 코덱은 CP949(확장 완성형)라 11,172자가 전부 통과한다 — *"서브셋해도 1,701KB"*라는
결론이 나올 뻔했다. 바이트 구역(0xB0A1~0xC8FE)을 직접 훑어 2,350자를 얻었다.
스크립트에 **2,350이 아니면 실패시키는 검사**를 박아뒀다.

축을 400~700으로 좁힌 근거는 `globals.css` 전수다 — 이 앱이 쓰는 굵기가 그 넷뿐이다(129KB 절약).

검산: 글꼴 `pretendard` `loaded` · 전송 **309KB** 한 파일 ·
1440px 4열 322px · 375px 카드 343px · lede 한 줄(26px) — **README 실측값이 전부 유지됐다.**

### 23:55 ❗ 아이콘을 손으로 옮기던 근거가 순환이었다

*"lucide-react도 깔고 아이콘 다시 봐줘"* 를 받고 깔았다. 그리고 내가 적어둔 근거를 다시 읽었다.

> *"쓰는 것이 다섯 개뿐이라 1,500개짜리 묶음을 들여올 재사용 압력이 아직 없다."*

**손으로 옮겨야 하니까 적게 쓴 것**인데 그 결과를 근거로 삼았다. 다른 근거였던
*"런타임 의존성이 4→5"*도 값을 안 재고 썼다 — 재보니 `.next/static/chunks`의 JS 합계가
**654KB → 657KB**, 아이콘 열한 개에 **3KB**다.

🔑 **패키지의 값은 여섯 개를 대신 넣어주는 데 있지 않다.**
바꾸고 더하는 값이 0에 가까워지는 것이 값이다. 깔자마자 두 가지가 바로 나왔다.

**① `[책으로 묶기]`와 `[책 보기]`가 같은 아이콘이었다.**
그때는 *"한 해에 둘 중 하나만 뜨니까 헷갈릴 일이 없다"*고 넘겼는데,
**책 줄이 격자라 둘이 나란히 뜬다.** 화면을 보면 바로 보이는 것이었고
실제로 이번에 2022년만 책이 있는 상태에서 나란히 뜬 것을 보고 확인했다.
`BookPlus`(없는 것을 만든다)와 `BookOpen`(있는 것을 연다)으로 갈랐다.

**② 아이콘이 있어야 하는데 없던 자리가 넷.**
주문(`Package`) · 다음 단계(`ArrowRight`) · 고치기(`SquarePen`) · 초기화(`RotateCcw`).
`Truck`이 아니라 `Package`인 이유는 **배송 상태를 만들지 않았기 때문**이다 —
트럭을 그리면 화면이 앱이 안 하는 일을 약속한다.

같이 규칙을 하나 적었다: **"어디로 가는가"와 "무엇에 대한 것인가"에만 붙인다.
"무엇을 하는가"에는 안 붙인다.** 저장·취소·다시 시도는 자리와 색이 이미 무게를 말한다.
전부에 붙이면 아이콘이 더는 무게를 못 만든다.

검산: 전 화면에서 **글자 없는 아이콘 0개** · 아이콘 11종이 각각 옆 글자와 짝지어 렌더됨.

## 2026-08-05

### 02:32 v2를 main에 얹었다 — 뼈대부터 다시

*"지금 너무 뼈대부터 이상해서 다 갈아엎어야 할 정도"* 를 받고 시작했다.
전면 개편은 **답에 따라 만드는 게 통째로 달라져서** 코드 전에 셋을 물었다 —
어디에 만들지(별도 브랜치) · 책·주문을 유지할지(유지, 사이드바 한 칸) ·
캘린더가 무엇을 할지(**예정 일정까지**).

세 번째가 가장 무거웠다. *"예정 일정"*은 곧 표준 검진·접종 일정을 저장소에 넣는다는 뜻이고
`⛔ 근거 없는 수치 금지`가 정면으로 걸린다. **그래서 코드보다 출처 조사가 먼저였다.**

🔴 **예방접종을 뺐다.** 가장 쓸모 있는 자료인데, 질병관리청 표를 직접 못 읽었고(404)
일정이 해마다 바뀐다. 틀린 접종 시기를 띄우는 대가가 크다.
넣으려면 필요한 것은 공식 표 경로와 **연도마다 갱신하는 사람**이고, 후자가 없다.

### 02:40 ❗ 재촉 장치를 만들 뻔한 자리

타임라인 꼬리(마지막 기록 → 오늘)를 그리면서 **경고로 그리기 쉬운 자리**를 지났다.
붉은색, 느낌표, `N일째 비어 있습니다`. 전부 안 넣었다.

🔑 `주기·마감·창 규칙을 만들지 않는다`가 **이 자리에서 가장 어기기 쉽다.**
화면은 얼마나 지났는지만 세고(`마지막으로 남긴 뒤로 17일`) 판단은 안 한다.
점선을 고른 것도 같은 이유다 — 실선이면 이어진 것처럼, 없으면 끝난 것처럼 읽힌다.

경계값을 따로 돌려 확인했다: `99일→99일` · `100일→3개월` · `24개월→2년` ·
**같은 날이면 null이고 화면이 `오늘 남기셨습니다`로 바뀐다.** `0일 지났습니다`는 화면이 할 말이 아니다.

### 02:45 ❗ 검사 도구가 틀린 값을 보여줬다

생일이 없는 경우를 확인하다가 머리말이 `하늘의 기록 · `로 끝나는 것처럼 보였다.
구분자만 남고 뒤가 빈 줄 — 진짜였으면 결함이다.

**내 정규식이 문제였다.** React가 텍스트 노드 사이에 `<!-- -->`를 넣는데
`[^<]*`가 거기서 멈췄다. 주석을 걷고 다시 파싱하니 `하늘의 기록 · 12점`으로 온전했다.

🔑 이 저장소가 *"결과가 0일 때 검사가 동작했다는 증거가 따로 필요하다"*를 배운 적이 있는데,
이번은 반대다 — **검사가 결함을 만들어냈다.** 고치기 전에 다시 잰 것이 맞았다.

---

## 시간 배분

| 구간 | 내용 | 시간 |
|---|---|---|
| 09:57~10:00 | 저장소 생성 | 3분 |
| 10:00~10:05 | 문제 정의 | 5분 |
| 10:05~10:10 | 사용자 정의 | 5분 |
| 10:10~10:14 | 인프라 이식 | 4분 |
| 10:14~10:22 | ❗ Docker 빌드 디버깅 | 8분 |
| ~10:37 | 시드 이미지 10점 확보(CC0/PD) | — |
| 10:37~10:45 | 시드 이미지 10장 열람 + ❗ 문장 전량 재작성 | 8분 |
| 10:45~10:51 | 스키마 3분할 + 마이그레이션 | 6분 |
| 10:51~10:57 | 시드 구현 + 멱등·클린 기동 검증 | 6분 |
| 병렬 | 타당성 조사(경제·법적) | — |

**8/2 누적 60분.**

### 2026-08-03

시각은 **커밋 타임스탬프**가 근거다 — `git log --format='%ad %s' --date=format:'%H:%M'`.
회상해서 못 쓰는 종류라 그때그때 적었다.

| 구간 | 내용 | 시간 |
|---|---|---|
| 10:53~11:10 | 문서 정합성 정리 · 시드 로직 분리 · 프로젝트 이름 통일 | 17분 |
| 11:10~11:37 | **Lv1 기반** — 사진 서빙 라우트 + 작품 목록(반응형 격자) | 27분 |
| 11:37~12:29 | 🔴 **공개 전 히스토리 정리** — 오염 13건 추적, `filter-repo`, 저장소 재생성 | 52분 |
| 12:29~13:36 | **Lv1 화면 4개** — 404 · 상세 · 등록 · 편집 (이슈→브랜치→PR→squash) | 67분 |
| 13:36~14:02 | README 골격 · 클린 클론 1차 · ❗ **이슈·PR 소실 복구** | 26분 |
| — | *(중단)* | — |
| 17:30~17:39 | **Lv3 실패 경로** — `error.tsx` · 죽은 크기 검사 · 프리렌더 | 9분 |
| 17:39~18:08 | **Lv2 주문 기능 5개** — 책·주문·목록·상태·초기화 | 29분 |
| 18:08~18:22 | README(레벨·설계 의도·AI 도구) · 문서 정합성 | 14분 |
| 18:22~19:06 | **최종 클린 클론** — GitHub URL에서, 빈 볼륨, 브라우저 왕복 | 44분 |
| — | *(중단)* | — |
| 23:06~23:12 | 문서-코드 불일치 5곳 | 6분 |
| 23:12~23:34 | 🔴 **동시 주문 2건 · 초기화 트랜잭션 · 예외 삼킴** | 22분 |
| 23:34~ | 알고 있는 한계 · README 재배치 · 시간 배분 | — |

### 2026-08-04

| 구간 | 내용 | 시간 |
|---|---|---|
| 14:13~15:23 | **전제 확장** — 주문 중복 열쇠 · 검색 오염 · 시간 축(생년월일) · 시드 12점 생성 | 70분 |
| 15:23~16:14 | 문서 전수 정정 · 1440px 실측 게이트 · README 수치 두 곳 | 51분 |
| — | *(중단)* | — |
| 16:24~19:00 | **화면 조사** — 실측(빈 폭 3곳·글자색 분포) · 아이콘/폰트/템플릿 라이선스 조사 | 36분 |
| 19:00~19:41 | **적용 4건** — 해 구간 · 상단 바+아이콘 · 가로 폭 · 강조색 | 41분 |
| 19:41~ | 전체 왕복(등록→책→주문→상태→초기화) · 문서 정합성 | — |

🔑 **조사에 쓴 36분 중 절반이 "고칠 것을 정하는" 게 아니라 "인상을 수치로 바꾸는" 데 갔다.**
`--accent`가 `--ink`와 같은 값이라는 것, 검색칸이 1263px이라는 것은
화면을 아무리 오래 봐도 안 보이고 재야 보인다. 그리고 재고 나니
**고칠 것 네 개가 저절로 정해졌다** — 고를 것이 없었다.

**성격별로 묶으면**

| 무엇에 | 대략 |
|---|---|
| 기획·문서 (문제·사용자·타당성·README·판단 기록) | **약 35%** |
| 구현 (Lv1 4화면 + Lv2 5기능 + Lv3 실패 경로) | **약 35%** |
| **검증·디버깅** (재현·실측·클린 클론·오염 검사) | **약 25%** |
| Docker·인프라 | **약 5%** |

🔑 **검증이 구현과 거의 같은 비중을 먹었다.** 그게 이 저장소에서 가장 많이 배운 부분이다 —
`bodySizeLimit`을 되돌려 죽은 코드를 재현한 것, 8.5MB 파일을 만들어 제출한 것,
두 창을 띄워 동시 제출을 쏜 것, DB를 껐다 켜고 [다시 시도]를 눌러본 것.
**전부 "고치는 시간"보다 "고쳤다는 걸 증명하는 시간"이 길었다.**

**인프라가 5%뿐인 이유**는 8/2에 검증해둔 것을 **다시 쓰지 않고 복사**했기 때문이다.
유일하게 손으로 새로 쓴 파일(`schema.prisma`)에서 8분을 잃었다(10:14 참조).

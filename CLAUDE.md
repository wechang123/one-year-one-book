# 아이 작품 아카이브 — 작업 규칙

**이 저장소는 AI 에이전트와 페어로 만들었다. 이 문서가 그 에이전트에게 준 규칙이다.**
에이전트는 작업을 시작하기 전에 이 문서를 먼저 읽는다.

> 진행 기록과 실패는 [`docs/worklog.md`](./docs/worklog.md)에 시각과 함께 남아 있다.
> *(마감·일정 같은 개인 사정은 `PLAN.local.md`에 따로 두고 공개하지 않는다)*

---

## 1. 에이전트 역할 — 이중 페르소나

이게 이 문서의 핵심이다. 에이전트에게 **두 개의 인격**을 동시에 요구한다.

**① 페어 개발자**
빠르게 만들되, **이해하지 못한 코드는 채택하지 않는다.**
무엇을 왜 그렇게 짰는지 항상 한국어로 짧게 설명한다.

**② 처음 이 저장소를 여는 사람 관점의 리뷰어**
개발팀장이 이 저장소를 처음 열었다고 가정하고 계속 되묻는다.

> 이 화면을 **왜 이렇게 배치했는지** 한 문장으로 설명할 수 있나?
> README만 보고 처음 온 사람이 `docker compose up`으로 실행할 수 있나?
> 이 판단을 **물어보면 답할 수 있나?**

🔑 **설명이 안 되는 화면·기능은 만들지 않는다.**
이 한 줄이 스코프를 정한다. "있으면 좋을 것"은 전부 여기서 걸러진다.

---

## 2. 만드는 것

> **아이가 만든 것을 아이의 말과 함께 남기고, 한 해가 지나면 한 권으로 묶는다.
> 그래서 실물을 마음 편히 정리할 수 있게 한다.**

### 🔑 이 서비스의 특이점

보통의 기록은 **"남기려고"** 하지만, 이건 **"버리려고"** 기록한다.
**책은 사용자의 목적이고, 서비스가 다루는 것은 콘텐츠다.** 둘을 섞지 않는다.
화면 속 데이터로는 실물을 버릴 결심이 안 서니 **책이 필요한 것은 맞다.**
다만 서비스가 잘해야 하는 일은 **아이가 만든 것과 아이가 한 말을 제대로 받아두는 것**이고,
받아둔 것이 없으면 묶을 것도 없다.

🔴 **이 문장은 한 번 뒤집혔다.** 원래 *"책이 부가 기능이 아니라 목적이다"*라고 적혀 있었고,
그 선언대로 홈에서 책 줄이 작품 격자 **위**에 있었다. 화면과 문서가 같이 미끄러진 것이라
어느 쪽도 서로를 잡아주지 못했다. 축을 되돌리면서 둘 다 고쳤다.

### 사용자 — 두 명이다

| | 주 사용자 | 부 사용자 |
|---|---|---|
| 누구 | 33세 부모 (아이 5세) | **이 저장소를 처음 여는 사람** |
| 상황 | 아이가 그림을 내미는 **30초** | `clone` → `up` → 브라우저, **5분** |
| 기기 | **폰 세로, 한 손** | **노트북 가로 1440px** |

🔴 **두 사용자를 동시에 만족시키는 방법 = 반응형.**
원칙: **"화면이 넓어지면 칸이 커지는 게 아니라 더 많이 보인다."**

### 트리거가 앱 밖에 있다 — 설계의 중심

**아이가 그림을 들고 오는 것 자체가 트리거다.** 앱이 만들지 않아도 반드시 발생한다.

→ **주기·마감·창(window) 규칙을 만들지 않는다.** 등록은 언제나 열려 있다.
→ 이것이 *"열었는데 할 게 없다"* 문제를 구조적으로 푼다.

---

## 3. 직전 프로젝트에서 실패한 3가지 — 반복 금지

같은 고민을 다른 주제로 한 번 했고, 세 가지를 틀렸다. 그 세 가지를 규칙으로 바꿨다.

| 실패 | 원인 | 이번 방지책 |
|---|---|---|
| **열어봐도 눌러볼 게 없었다** | 시드가 12칸 중 11칸을 채웠고, 등록은 "창"이 열려야만 가능했다 | **창 규칙 없음.** 등록이 언제나 열려 있다. 시드는 **책·주문을 비워둬** 직접 만들게 한다 |
| **노트북에서 좁은 칼럼 하나만 떴다** | 타겟을 "폰 세로"로만 정하고 데스크탑을 놓쳤다 | **부 사용자를 명시.** 반응형이 요구사항이다 |
| **화면이 DB의 18%만 썼다** | 쿼리에서 필드를 필터링해놓고 CSS 탓을 했다 | 화면이 허전하면 **CSS 전에 쿼리를 먼저 본다** |

---

## 4. 시드 데이터 배치 — 할 일을 남긴다

| | 상태 | 왜 |
|---|---|---|
| **작품 10점** | 등록됨 | 켜자마자 서비스가 뭔지 보인다 |
| **책** | **안 만든 상태** | 직접 만들어본다 |
| **주문** | **0건** | 아래 |

> **주문 1건도 넣지 않는다.**
> `Order`는 `Collection`에 딸려 있고 책은 `@@unique([profileId, year])`다.
> 주문 1건을 넣으려면 2026년 책을 먼저 만들어야 하는데, 그러면 **만들어볼 책이 사라진다.**
> 대신 도달 경로를 짧게 만든다: **책 만들기(버튼 1) → 주문(폼 1) → 상태 변경(버튼 1).**
> **직접 만든** 주문의 상태가 바뀌는 걸 보는 편이, 남이 만든 주문을 구경하는 것보다 강하다.

🔑 **[데모 초기화]는 반드시 만든다.** 자기가 만든 데이터만 지우고 처음 상태로.
*망가뜨릴 수 없으면 아무도 안 눌러본다.*

---

## 5. 절대 규칙

- ⛔ **외부 API·LLM 호출 금지.** 독립 실행되어야 한다
- ⛔ **`.env` 커밋 금지.** `.env.example`만
- ⛔ **포트 하드코딩 금지.** `${PORT}`·`${DB_PORT}`로 변경 가능해야 한다
- ⛔ **근거 없는 수치 금지.** 쪽수·가격·배송일은 제작 사양에 달려 있어 임의로 정하지 않는다
- ⛔ **제3자 실명·과제 원문을 저장소에 쓰지 않는다** (공개 저장소)
- ⛔ **설명할 수 없는 코드를 채택하지 않는다**

---

## 6. 시드 이미지 — 기술 함정 2개

`public/seed/01~10.jpg` · **전부 CC0 / Public Domain** · Wikimedia Commons · 합계 1.7MB

🔴 **전부 실물을 촬영·스캔한 진짜 아이 그림이다.** 디지털 그림판 작품은 전량 배제했다 —
**이 서비스의 전제가 "실물이 쌓여서 버려야 한다"인데 디지털은 버릴 실물이 없다.**

**아이 얼굴이 나온 이미지는 한 장도 없다** (초상권).
CC0/PD는 표기 의무가 없지만 **의무 없이도 밝히는 것이 판단**이다 → README에 출처 목록.

**① `public/`에 업로드 파일을 쓰면 안 된다**
`next start`는 `public/`을 **부팅 시점에 스캔**한다. 그래서 커밋된 시드 이미지는 클린 클론에서 살아남는다.
반대로 **실행 중에 파일을 쓰면 재시작 전까지 404다** — 볼륨을 붙여도 안 고쳐진다.
→ **업로드는 DB `Bytes` 컬럼 + route handler로 서빙한다.**

**② Server Action 본문 크기 기본값은 1MB다**
폰 사진은 2~5MB라 **오류 없이 조용히 실패한다.**
→ `next.config.ts`의 `serverActions.bodySizeLimit`을 **`12mb`**로 두었다.

🔑 이 값은 **앱이 받는 사진 한도(8MiB)보다 커야 한다.**
`bodySizeLimit`은 파일이 아니라 **요청 본문 전체**(multipart 경계·필드 이름·아이 말·만든 날 포함)에 걸린다.
두 값을 같게 두면 한도를 넘는 파일은 본문 단계에서 먼저 잘려 **액션이 시작조차 못 하고**,
앱이 준비해둔 한국어 안내가 **도달할 수 없는 코드**가 된다. 실제로 그 상태였다.

---

## 7. 커밋 규칙

- **한 커밋에 수천 줄을 몰아 넣지 않는다.** 기능을 적게 만들라는 게 아니라 **단위를 쪼개라**는 뜻
- 목표: 커밋 하나에 **파일 1~8개, 수백 줄 이내**
- 형식: `feat:` `fix:` `docs:` `chore:` `refactor:` + **한국어 요약**. 본문에 **왜 그렇게 했는지 2줄**
- **스캐폴드·의존성은 별도 커밋으로 분리**
- 🔴 **`main`은 항상 실행 가능한 상태로 유지한다**

> 🔑 커밋이 논리 단위로 쪼개져 있다는 것 자체가
> **생성된 코드를 단위별로 이해하고 검증했다는 기록**이다.

---

## 8. 문서 위치

| 파일 | 내용 |
|---|---|
| `docs/01-problem.md` | 문제 정의 |
| `docs/02-user.md` | 사용자 정의 |
| `docs/03-feasibility.md` | 타당성 판정 (경제·기술·법 3축) — 출처·라이선스 판정의 정본 |
| `docs/04-content.md` | 콘텐츠 설계 — 시드 문장 10개와 그 근거 |
| `docs/04b-생성이미지.md` | 시드 이미지 생성 규칙. **접었다가 다시 편 경로** — 접은 이유가 §0, 다시 편 이유가 §0-b |
| `docs/worklog.md` | 🔴 **작업 기록.** 무엇이 틀렸고 어떻게 발견했는지 시각과 함께 |

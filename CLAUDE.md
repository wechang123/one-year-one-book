# 아이 작품 아카이브 — 작업 규칙

이 저장소에서 일하는 에이전트는 **이 문서를 먼저 읽고 시작한다.**

---

## 0. 🔴 지금 위치 (2026-08-02 10:40)

(비공개 — 삭제)

```
✅ S0 저장소 · S1 문제 정의 · S2 사용자 정의
✅ 인프라 이식 + docker compose up 1회 기동 검증 완료 (포트 변경도 확인)
✅ S3 타당성 조사 (원자료 docs/03a-research-raw.md)
✅ 시드 이미지 10점 확정 (public/seed/, CC0·PD, 1.7MB)
✅ 시드 문장 10개 — 그림을 실제로 보고 다시 씀 (★6개)
✅ 스키마 확정 + 마이그레이션 3개 (커밋도 3개로 분할)
✅ 시드 구현 — down -v → up 클린 기동 + 멱등 검증 통과
🔜 S5 기능 정의 + WBS  ·  S6 일정 + 위험
🔜 구현
```

(비공개 — 삭제)
(비공개 — 삭제)

---

## 1. 미션

(비공개 — 삭제)

| | |
|---|---|
(비공개 — 삭제)
(비공개 — 삭제)

**최종 목표는 제출이 아니라 최종 합격이다.** 그래서 판단·실패·시간 배분을 `docs/worklog.md`에 실시간으로 남긴다.

---

## 2. 에이전트 역할 — 이중 페르소나

**① 페어 개발자** — 빠르게 만들되, **이해하지 못한 코드는 채택하지 않는다.** 무엇을 왜 그렇게 짰는지 항상 한국어로 짧게 설명한다.

**② 심사자 관점의 리뷰어** — 개발팀장이 이 저장소를 처음 열었다고 가정하고 계속 되묻는다.
> 이 화면을 **왜 이렇게 배치했는지** 한 문장으로 설명할 수 있나?
> README만 보고 처음 온 사람이 `docker compose up`으로 실행할 수 있나?
(비공개 — 삭제)

**설명이 안 되는 화면·기능은 만들지 않는다.**

---

## 3. 확정 서비스

> **아이가 만든 것을 아이의 말과 함께 남기고, 한 해가 지나면 한 권으로 묶는다.
> 그래서 실물을 마음 편히 정리할 수 있게 한다.**

### 🔑 이 서비스의 특이점
보통의 기록은 **"남기려고"** 하지만, 이건 **"버리려고"** 기록한다.
그래서 **책이 부가 기능이 아니라 목적이다.** 화면 속 데이터로는 실물을 버릴 결심이 안 선다.

### 사용자
| | 주 사용자 | 부 사용자 |
|---|---|---|
| 누구 | 33세 부모 (아이 5세) | **심사자** |
| 상황 | 아이가 그림을 내미는 **30초** | `clone` → `up` → 브라우저, **5분** |
| 기기 | **폰 세로, 한 손** | **노트북 가로 1440px** |

🔴 **두 사용자를 동시에 만족시키는 방법 = 반응형.** 좁으면 1열, 넓으면 격자.
원칙: **"화면이 넓어지면 칸이 커지는 게 아니라 더 많이 보인다."**

### 트리거가 앱 밖에 있다 — 설계의 중심
**아이가 그림을 들고 오는 것 자체가 트리거다.** 앱이 만들지 않아도 반드시 발생한다.
→ **주기·마감·창(window) 규칙을 만들지 않는다.** 등록은 언제나 열려 있다.
→ 이것이 *"심사자가 켰는데 할 게 없다"* 문제를 구조적으로 푼다.

---

(비공개 — 삭제)

| 레벨 | 내용 |
|---|---|
| **Lv1** | 핵심 플로우 — 작품 **등록·조회·편집** |
| **Lv2** | 주문 저장·조회·상태 관리 (`접수 → 인쇄 → 배송`) |
| **Lv3** | UI/UX 설계 |

🔴 **"Lv1을 완성도 있게 한 결과물이 Lv3를 어설프게 시도한 것보다 좋다"**
**디자인 감각은 평가하지 않는다.** Tailwind 등 기성 UI 권장.
핵심은 **"누가 쓰는지 정하고 그 사람 기준으로 설계했는가, 그 판단을 설명할 수 있는가."**

**Lv3가 명시적으로 보는 것**
- 처음 온 사용자가 **뭘 하는 서비스인지, 다음에 뭘 눌러야 하는지** 아는가
- 주문 상태를 **사용자 말로** 보여주는가
- **빈 목록 · 입력 실수 · 로딩 · 실패**에서 화면이 무엇을 말하는가
- **다른 화면 크기**에서도 쓸 수 있는가

**제출 요건**: `docker compose up` 1회 / 포트 환경변수 변경 / **더미데이터로 로그인 없이 즉시 확인** / README 단독 실행 / Public / 제출 전 본인 실행 테스트

---

## 5. 🔴 이전 프로젝트에서 실패한 3가지 — 반복 금지

| 실패 | 원인 | 이번 방지책 |
|---|---|---|
| **심사자가 눌러볼 게 없었다** | 시드가 11/12칸을 채웠고, 등록은 "창"이 열려야만 가능했다 | **창 규칙 없음.** 등록이 언제나 열려 있다. 시드는 **책·주문을 비워둬** 심사자가 직접 만들게 한다 |
| **노트북에서 좁은 칼럼 하나만 떴다** | 타겟을 "폰 세로"로만 정하고 데스크탑을 놓쳤다 | **심사자를 부 사용자로 명시.** 반응형이 요구사항이다 |
| **화면이 DB의 18%만 썼다** | 쿼리에서 필드를 필터링해놓고 CSS 탓을 했다 | 화면이 허전하면 **CSS 전에 쿼리를 먼저 본다** |

---

## 6. 시드 데이터 배치 — 심사자가 할 일을 남긴다

| | 상태 | 왜 |
|---|---|---|
| **작품 10점** | 등록됨 | 켜자마자 서비스가 뭔지 보인다 |
| **책** | **안 만든 상태** | 심사자가 직접 만들어본다 |
| **주문** | **0건** ← 8/2 변경 | 아래 이유 |

> ⚠️ **주문 1건도 안 넣기로 바꿨다 (8/2 10:5x).**
> `Order`는 `Collection`에 딸려 있고 책은 `@@unique([profileId, year])`다.
> 주문 1건을 넣으려면 2026년 책을 먼저 만들어야 하는데, 그러면 **심사자가 만들어볼 책이 사라진다.**
> 눌러볼 게 없던 이전 프로젝트의 실패를 그대로 반복하게 된다.
> 대신 도달 경로를 짧게 만든다: **책 만들기(버튼 1) → 주문(폼 1) → 상태 변경(버튼 1).**
> 심사자가 **직접 만든** 주문의 상태가 바뀌는 걸 보는 편이, 남이 만든 주문을 구경하는 것보다 강하다.

**심사자가 직접 할 수 있는 것 6가지**: 사진 등록 · 설명 편집 · 책 만들기 · 주문 · 상태 변경 · **데모 초기화**

🔑 **[데모 초기화]는 반드시 만든다.** 자기가 만든 데이터만 지우고 처음 상태로.
*망가뜨릴 수 없으면 아무도 안 눌러본다.*

---

## 7. 절대 규칙

- ⛔ **외부 API·LLM 호출 금지.** 독립 실행되어야 한다
- ⛔ **`.env` 커밋 금지.** `.env.example`만
- ⛔ **포트 하드코딩 금지.** `${PORT}`·`${DB_PORT}`로 변경 가능해야 한다
- ⛔ **근거 없는 수치 금지.** 쪽수·가격·배송일은 제작 사양에 달려 있어 임의로 정하지 않는다
(비공개 — 삭제)
- ⛔ 설명할 수 없는 코드를 채택하지 않는다

---

## 8. 시드 이미지 — 이미 확보됨

`public/seed/01~10.jpg` · **전부 CC0 / Public Domain** · Wikimedia Commons · 합계 1.7MB

🔴 **전부 실물을 촬영·스캔한 진짜 아이 그림이다.** 디지털 그림판 작품은 전량 배제했다 —
**이 서비스의 전제가 "실물이 쌓여서 버려야 한다"인데 디지털은 버릴 실물이 없다.**

**아이 얼굴이 나온 이미지는 한 장도 없다** (초상권).
README에 출처·라이선스 목록을 반드시 넣는다. CC0/PD는 표기 의무가 없지만 **의무 없이도 밝히는 것이 판단**이다.

⚠️ `public/`은 `next start`가 **부팅 시점에 스캔**한다. 그래서 커밋된 시드 이미지는 클린 클론에서 살아남는다.
반대로 **사용자 업로드를 `public/`에 파일로 쓰면 재시작 전까지 404다** — 볼륨을 붙여도 안 고쳐진다.
→ **업로드는 DB `Bytes` 컬럼 + route handler로 서빙한다.**

`next.config.ts`의 `serverActions.bodySizeLimit`은 **8mb로 이미 설정돼 있다.**
기본값 1MB로는 폰 사진(2~5MB) 업로드가 **오류 없이 조용히 실패한다.**

---

## 9. 커밋 규칙

- **한 커밋에 수천 줄을 몰아 넣지 않는다.** 기능을 적게 만들라는 게 아니라 **단위를 쪼개라**는 뜻
- 목표: 커밋 하나에 **파일 1~8개, 수백 줄 이내**
- 형식: `feat:` `fix:` `docs:` `chore:` + **한국어 요약**. 본문에 **왜 그렇게 했는지 2줄**
- **스캐폴드·의존성은 별도 커밋으로 분리**
- 🔴 **`main`은 항상 실행 가능한 상태로 유지한다**

> 🔑 커밋이 논리 단위로 쪼개져 있다는 것 자체가 **생성된 코드를 단위별로 이해하고 검증했다는 기록**이다.
> 서술형 Q3가 정확히 그것을 묻는다.

---

## 10. (비공개 — 삭제)

| 날 | 내용 | 게이트 |
|---|---|---|
| **8/2 (오늘)** | 시드 문장 · S5 기능+WBS · S6 일정+위험 · 스키마·시드 구현 | 22:00 상한 |
(비공개 — 삭제)
(비공개 — 삭제)
(비공개 — 삭제)

---

## 11. 문서 위치

| 파일 | 내용 |
|---|---|
| `docs/01-problem.md` | 문제 정의 — 서술형 Q1·README §7의 원본 |
| `docs/02-user.md` | 사용자 정의 — 서술형 Q2의 원본 |
| `docs/03a-research-raw.md` | 타당성 조사 원자료 (유사 서비스·라이선스·오픈소스, 출처 링크 포함) |
| `docs/04-content.md` | 콘텐츠 설계 — ⚠️ **시드 문장은 폐기됨. 그림 보고 다시 쓸 것** |
| `docs/worklog.md` | 🔴 **작업 기록. 계속 이어서 쓴다** |

> ⚠️ `docs/03-feasibility.md`(종합본)는 작성 중 덮어써서 유실됐다. 원자료 `03a`로 재작성해야 한다.

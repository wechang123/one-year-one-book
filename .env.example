# docker-compose는 이 파일 없이도 동작한다(compose 안에 기본값이 박혀 있다).
# 이 값은 호스트에서 prisma CLI를 직접 쓸 때만 필요하다.
DATABASE_URL="postgresql://kidarchive:kidarchive@localhost:5433/kidarchive?schema=public"

# 호스트 포트만 바꾼다. 컨테이너 안은 항상 3000.
# PORT=3000
# DB_PORT=5433
